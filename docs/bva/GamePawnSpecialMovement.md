# BVA Analysis for Game Pawn Special Movement

## Method under test: `movePiece(startRow, startCol, endRow, endCol)`

| ID | Method(s) under test | System under test | Expected output | Implemented? |
|---|---|---|---|---|
| TC1 | `movePiece`, `getBoard`, `getCurrentTurn` | WHITE pawn moves two squares from starting row with clear path: WHITE pawn at `(6,0)`, squares `(5,0)` and `(4,0)` empty, then `movePiece(6,0,4,0)` is called | Move succeeds, pawn occupies `(4,0)`, start square `(6,0)` is empty, turn switches to BLACK | :white_check_mark: |
| TC2 | `movePiece`, `getBoard`, `getCurrentTurn` | BLACK pawn moves two squares from starting row with clear path after WHITE moves first: WHITE moves `(6,7)` to `(5,7)`, then BLACK pawn moves `(1,0)` to `(3,0)` with `(2,0)` and `(3,0)` empty | Move succeeds, BLACK pawn occupies `(3,0)`, start square `(1,0)` is empty, turn switches to WHITE | :white_check_mark: |
| TC3 | `movePiece`, `getBoard`, `getCurrentTurn` | WHITE pawn attempts two-square move with intermediate square blocked: WHITE pawn at `(6,0)`, blocker at `(5,0)`, destination `(4,0)` empty, then `movePiece(6,0,4,0)` is called | Move is rejected with exception, board unchanged, turn remains WHITE  | :white_check_mark: |
| TC4 | `movePiece`, `getBoard`, `getCurrentTurn` | WHITE pawn attempts two-square move with destination occupied: WHITE pawn at `(6,0)`, square `(5,0)` empty, BLACK pawn at `(4,0)`, then `movePiece(6,0,4,0)` is called | Move is rejected with exception, board unchanged, turn remains WHITE | :white_check_mark: |
| TC5 | `movePiece`, `getBoard`, `getCurrentTurn` | WHITE pawn attempts two-square move after leaving starting row: WHITE pawn is placed at `(5,0)`, squares `(4,0)` and `(3,0)` empty, then `movePiece(5,0,3,0)` is called | Move is rejected with exception, board unchanged, turn remains WHITE | :white_check_mark: |
| TC6 | `movePiece`, `getBoard`, `getCurrentTurn` | WHITE pawn captures diagonally: WHITE pawn is placed at `(4,4)`, BLACK pawn is placed at `(3,5)`, then `movePiece(4,4,3,5)` is called | BLACK pawn is removed, WHITE pawn occupies `(3,5)`, start square `(4,4)` is empty, turn switches to BLACK | :white_check_mark: |
| TC7 | `movePiece`, `getBoard`, `getCurrentTurn` | WHITE pawn attempts diagonal move into empty square: WHITE pawn is placed at `(4,4)`, destination `(3,5)` is empty, then `movePiece(4,4,3,5)` is called | Move is rejected with exception, board unchanged, turn remains WHITE | :white_check_mark: |
| TC8 | `movePiece`, `getBoard`, `getCurrentTurn` | WHITE pawn attempts forward move into occupied square: WHITE pawn is placed at `(4,4)`, BLACK pawn is placed at `(3,4)`, then `movePiece(4,4,3,4)` is called | Move is rejected with exception, board unchanged, turn remains WHITE | :x: |
| TC9 | `movePiece`, `getBoard`, `getCurrentTurn` | BLACK pawn captures diagonally after WHITE moves first: BLACK pawn is placed at `(3,3)`, WHITE pawn is placed at `(4,4)`, WHITE moves `(6,7)` to `(5,7)`, then `movePiece(3,3,4,4)` is called | WHITE pawn is removed, BLACK pawn occupies `(4,4)`, start square `(3,3)` is empty, turn switches to WHITE | :x: |
