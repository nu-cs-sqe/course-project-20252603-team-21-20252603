# BVA Analysis for Game Self-Check Validation

## Method under test: `movePiece(startRow, startCol, endRow, endCol)`

| ID | Method(s) under test | System under test | Expected output | Implemented? |
|---|---|---|---|---|
| TC1 | `movePiece`, `getBoard`, `getCurrentTurn`, `isKingInCheck` | White rook moves from a1 to a2 while white king on e1 is not in check and the move does not expose check | Move succeeds; rook moves to a2; a1 empty; `isKingInCheck(WHITE)` returns `false`; turn switches to BLACK | :x: |
| TC2 | `movePiece`, `getBoard`, `getCurrentTurn`, `isKingInCheck` | White bishop on e2 moves away from e-file, exposing white king on e1 to black rook on e8; e3/e4/e5/e6/e7 clear | Move rejected; bishop stays e2; destination remains unchanged; `isKingInCheck(WHITE)` returns `false`; turn remains WHITE | :x: |
| TC3 | `movePiece`, `getBoard`, `getCurrentTurn`, `isKingInCheck` | White king on e1 attempts to move to e2 where black rook on e8 attacks down open e-file; e3/e4/e5/e6/e7 clear | Move rejected; king stays e1; e2 empty; turn remains WHITE | :x: |
| TC4 | `movePiece`, `getBoard`, `getCurrentTurn`, `isKingInCheck` | White king on e1 captures adjacent black rook on e2, and no black piece attacks e2 after the capture | Move succeeds; king moves to e2; black rook removed; e1 empty; `isKingInCheck(WHITE)` returns `false`; turn switches to BLACK | :x: |
| TC5 | `movePiece`, `getBoard`, `getCurrentTurn`, `isKingInCheck` | White king on e1 attempts to capture black rook on e2, but black bishop on h5 defends e2 diagonally; path g4/f3 clear | Move rejected; king stays e1; black rook remains e2; turn remains WHITE | :x: |
| TC6 | `movePiece`, `getBoard`, `getCurrentTurn`, `isKingInCheck` | White king on e1 is in check from black rook on e8; white bishop moves from d1 to e2 to block the e-file; e3/e4/e5/e6/e7 clear | Move succeeds; bishop moves to e2; d1 empty; `isKingInCheck(WHITE)` returns `false`; turn switches to BLACK | :x: |
| TC7 | `movePiece`, `getBoard`, `getCurrentTurn`, `isKingInCheck` | White king on e1 is in check from black rook on e8; white knight moves from g1 to f3 and does not block, capture, or move the king out of check | Move rejected; knight stays g1; f3 empty; `isKingInCheck(WHITE)` remains `true`; turn remains WHITE | :x: |
| TC8 | `movePiece`, `getBoard`, `getCurrentTurn`, `isKingInCheck` | White king on e1 is in check from black rook on e8; white rook moves from h8 to e8 and captures the checking rook | Move succeeds; white rook moves to e8; black rook removed; h8 empty; `isKingInCheck(WHITE)` returns `false`; turn switches to BLACK | :x: |
| TC9 | `movePiece`, `getBoard`, `getCurrentTurn`, `isKingInCheck` | White king on e1 is in check from black knight on f3; white bishop moves from c4 to e2, which would block a line attack but does not resolve the knight check | Move rejected; bishop stays c4; e2 empty; `isKingInCheck(WHITE)` remains `true`; turn remains WHITE | :x: |
| TC10 | `movePiece`, `getBoard`, `getCurrentTurn`, `isKingInCheck` | Black bishop on e7 moves away from e-file, exposing black king on e8 to white rook on e1; e2/e3/e4/e5/e6 clear; current turn is BLACK | Move rejected; bishop stays e7; destination remains unchanged; `isKingInCheck(BLACK)` returns `false`; turn remains BLACK | :x: |
| TC11 | `movePiece`, `getBoard`, `getCurrentTurn`, `isKingInCheck` | Black king on e8 is in check from white rook on e1; black bishop moves from d8 to e7 to block the e-file; e2/e3/e4/e5/e6 clear; current turn is BLACK | Move succeeds; bishop moves to e7; d8 empty; `isKingInCheck(BLACK)` returns `false`; turn switches to WHITE | :x: |
| TC12 | `movePiece`, `getBoard`, `getCurrentTurn`, `isKingInCheck` | White rook on e2 is pinned between white king on e1 and black rook on e8; rook moves from e2 to e8 and captures the attacking rook | Move succeeds; white rook moves to e8; black rook removed; e2 empty; `isKingInCheck(WHITE)` returns `false`; turn switches to BLACK | :x: |